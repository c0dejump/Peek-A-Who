# ── Core framework ─────────────────────────────────────────────
flask>=3.0
python-dotenv>=1.0

# ── LLM integration ────────────────────────────────────────────
litellm>=1.40          # multi-provider LLM (Ollama, OpenAI, Anthropic, Groq, Gemini…)

# ── HTTP / scraping ────────────────────────────────────────────
requests>=2.31
beautifulsoup4>=4.12   # HTML parsing (DDG search, platform detection, web_archive)
lxml>=5.0              # faster BS4 parser (optional but recommended)

# ── OSINT tools ────────────────────────────────────────────────
maigret>=0.4.4         # cross-platform username scanner (36+ targeted sites)
sherlock-project>=0.15 # cross-platform username scanner (parallel with maigret)
ghunt>=2.2             # Google account OSINT (Gmail → Gaia ID, Maps reviews)
phonenumbers>=8.13     # phone parsing, carrier, region, line type
ignorant>=1.0          # phone → social platform registration (WhatsApp, Telegram…)

# ── Domain / WHOIS ─────────────────────────────────────────────
python-whois>=0.9      # WHOIS lookups for Watson whois_lookup tool

# ── MCP server (external tool-calling integrations) ────────────
mcp>=1.0               # Model Context Protocol — used by mcp_server.py only

# ── Optional / enhanced features ───────────────────────────────
# playwright>=1.44     # JavaScript-rendered page fallback (Instagram, TikTok)
selenium>=4.20         # real-browser Google dorking (skills/utils/browser_search.py); needs a Chromium/Firefox binary
# pimeyes             # paid facial recognition — manual integration only
#
# Face recognition (optional — feature degrades gracefully if not installed):
#   pip install cmake dlib face_recognition
# face_recognition
Pillow>=10.0            # EXIF GPS/date extraction for geotime photos
