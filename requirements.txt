# Core web framework
flask>=3.0
python-dotenv>=1.0

# HTTP & HTML parsing
requests>=2.31
beautifulsoup4>=4.12

# Phone number parsing
phonenumbers>=8.13

# MCP (Model Context Protocol) — LLM tool orchestration
mcp>=1.5

# LLM integration (supports OpenAI, Anthropic, Ollama, Groq, Gemini, etc.)
litellm>=1.40

# ── Optional external CLI tools (install separately) ──────────────────────────
# maigret (500+ site cross-platform username search):   pip install maigret
# ignorant (phone social registration check):           pip install ignorant
# ghunt (Google account OSINT):                         pip install ghunt
# phoneinfoga (phone OSINT binary): https://github.com/sundowndev/phoneinfoga/releases
